name: Test Jenesis Tool

on:
  push:
    branches: [main]
  pull_request:

jobs:
  demos:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        # internal-module and external-module are omitted until the next
        # build.jenesis release: they load a build module through the
        # class-loader bridge, which currently rejects the overlay's older
        # build.jenesis against the local sources.
        #
        # The `-as-modular` entries force the pure MODULAR layout
        # (-Djenesis.project.layout=modular) on the same sources that otherwise
        # auto-detect MODULAR_TO_MAVEN, so both resolves are exercised. The
        # `*-executable` demos run their build/Run.java launcher, which stages a
        # jpackage app-image and runs it. The `-jmod-jlink` entry enables the jmod
        # and jlink steps and stages their results (a .jmod beside the modular jar
        # and a jlink runtime image under stage/runtime).
        demo:
          - { name: java-pom, dir: java-pom, entry: build/jenesis/Project.java }
          - { name: java-modular, dir: java-modular, entry: build/jenesis/Project.java }
          - { name: java-modular-as-modular, dir: java-modular, entry: build/jenesis/Project.java, flags: -Djenesis.project.layout=modular }
          - { name: java-pom-multi, dir: java-pom-multi, entry: build/jenesis/Project.java }
          - { name: java-modular-multi, dir: java-modular-multi, entry: build/jenesis/Project.java }
          - { name: java-modular-multi-as-modular, dir: java-modular-multi, entry: build/jenesis/Project.java, flags: -Djenesis.project.layout=modular }
          - { name: java-modular-jmod-jlink, dir: java-modular, entry: build/jenesis/Project.java stage, flags: -Djenesis.java.jmod=true -Djenesis.java.jlink=true }
          - { name: java-pom-executable, dir: java-pom-executable, entry: build/Run.java }
          - { name: java-modular-executable, dir: java-modular-executable, entry: build/Run.java }
          - { name: java-modular-executable-as-modular, dir: java-modular-executable, entry: build/Run.java, flags: -Djenesis.project.layout=modular }
          - { name: kotlin, dir: kotlin, entry: build/jenesis/Project.java }
          - { name: scala, dir: scala, entry: build/jenesis/Project.java }
          - { name: groovy, dir: groovy, entry: build/jenesis/Project.java }
          - { name: custom-assembler, dir: custom-assembler, entry: build/Demo.java }
          - { name: custom-jmod, dir: custom-jmod, entry: build/Demo.java }
          - { name: custom-build, dir: custom-build, entry: build/Demo.java }
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up JDK 25
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5.2.0
        with:
          distribution: temurin
          java-version: '25'

      - name: Demo ${{ matrix.demo.name }}
        shell: bash
        working-directory: demo/${{ matrix.demo.dir }}
        run: java ${{ matrix.demo.flags }} ${{ matrix.demo.entry }}

      - name: Upload target on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: target-demo-${{ matrix.os }}-${{ matrix.demo.name }}
          path: demo/${{ matrix.demo.dir }}/target/
          retention-days: 3
          if-no-files-found: ignore

  project:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        layout: [auto, maven, modular, modular_to_maven]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up JDK 25
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5.2.0
        with:
          distribution: temurin
          java-version: '25'

      - name: Project layout ${{ matrix.layout }}
        shell: bash
        run: java -Djenesis.project.layout=${{ matrix.layout }} build/jenesis/Project.java stage

      - name: Upload target on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: target-project-${{ matrix.os }}-${{ matrix.layout }}
          path: target/
          retention-days: 3
          if-no-files-found: ignore

  sdk:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up JDK 25
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5.2.0
        with:
          distribution: temurin
          java-version: '25'

      - name: Stage Jenesis
        shell: bash
        run: |
          java -Djenesis.project.version=0.0.0-TEST \
               -Djenesis.project.sources=true \
               -Djenesis.project.skipTests=true \
               -Djenesis.project.metadata=project.properties \
               build/jenesis/Project.java stage

      - name: Populate SDK layout
        shell: bash
        run: |
          mkdir -p sdk/lib sdk/sources
          cp target/stage/maven/output/build/jenesis/build.jenesis/0.0.0-TEST/build.jenesis-0.0.0-TEST.jar sdk/lib/
          cp target/stage/maven/output/build/jenesis/build.jenesis/0.0.0-TEST/build.jenesis-0.0.0-TEST-sources.jar sdk/sources/

      - name: Run SDK tests (POSIX)
        if: runner.os != 'Windows'
        run: bash sdk-tests/test.sh

      - name: Run SDK tests (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: sdk-tests\test.bat

      - name: Upload target on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: target-sdk-${{ matrix.os }}
          path: target/
          retention-days: 3
          if-no-files-found: ignore
